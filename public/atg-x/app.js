const root=document.querySelector("#app");const toastNode=document.querySelector("#toast");const DEVICE_KEY="atgx_device";let games=[];let selectedGame="戰神賽特2";function escapeHtml(value){return String(value??"").replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[char])}function deviceId(){let id=localStorage.getItem(DEVICE_KEY);if(!id){id=crypto.randomUUID()+crypto.randomUUID();localStorage.setItem(DEVICE_KEY,id)}return id}async function api(url,options={}){const response=await fetch(url,{...options,cache:"no-store",headers:{"x-atgx-device":deviceId(),...(options.method?{"x-atgx-client":"1"}:{}),...(options.body?{"content-type":"application/json"}:{}),...(options.headers||{})}});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||`HTTP ${response.status}`);return data}function toast(message){toastNode.textContent=message;toastNode.classList.add("show");clearTimeout(toastNode.timer);toastNode.timer=setTimeout(()=>toastNode.classList.remove("show"),3000)}function formatNumber(value,suffix=""){const number=Number(value);return Number.isFinite(number)?`${number.toLocaleString("zh-TW",{maximumFractionDigits:2})}${suffix}`:"—"}function login(){root.innerHTML=`<section class="hero"><div class="hero-copy"><p class="eyebrow">ATG INTELLIGENCE・SERIAL ACCESS</p><h1>ATG AI <span>預測X輔助程式</span></h1><p>專注 ATG 電子遊戲的即時選房、資料訊號與資金節奏輔助。輸入管理員提供的專屬序號，即可啟用你的分析介面。</p><form class="login-card" id="activate"><label>輸入啟用序號</label><div class="serial-row"><input name="serial" autocomplete="off" spellcheck="false" placeholder="ATGX-XXXX-XXXX-XXXX-XXXX-XXXX-XXXX" required><button class="primary">安全啟用</button></div><small>每組序號限綁定一個瀏覽器裝置。請勿分享序號；清除瀏覽器資料前請先聯絡管理員。</small></form></div><div class="hero-logo"><img src="/atg-x/assets/atg-x-logo.webp" alt="ATG駭客"></div></section>`}function gameCard(game){return `<button class="game ${game.ready?"ready":""} ${game.gameName===selectedGame?"active":""}" data-game="${escapeHtml(game.gameName)}"><img src="${escapeHtml(game.image)}" alt=""><span class="game-body"><b>${escapeHtml(game.gameName)}</b><span><i></i>${game.ready?`即時同步・${game.availableRooms} 房可分析`:"資料同步中"}</span></span></button>`}function dashboard(me){root.innerHTML=`<section class="section-head"><div><p class="eyebrow">ATG X COMMAND CENTER</p><h1>即時戰術分析</h1><p>選擇遊戲，核對資料訊號，再依固定風險單位操作。</p></div><div class="license-state">授權至 ${new Date(me.expiresAt).toLocaleDateString("zh-TW")}</div></section><section class="game-grid" id="games">${games.map(gameCard).join("")}</section><section class="workspace"><div class="panel"><h2>分析設定</h2><div class="control"><label>目前遊戲</label><input id="gameName" value="${escapeHtml(selectedGame)}" readonly></div><div class="control"><label>本次操作本金（選填）</label><input id="bankroll" inputmode="numeric" placeholder="例如 10000"></div><button class="primary" id="analyze">啟動 AI 訊號掃描</button><button class="ghost" id="logout" style="width:100%;margin-top:10px">登出本裝置</button></div><div class="result" id="result"><div class="empty-result"><div><p class="eyebrow">READY TO SCAN</p><b>等待啟動分析</b><p>系統只會採用已完成即時核對的空房資料</p></div></div></div></section>`}function resultCard(result){return `<div class="radar"><div class="room"><small>${escapeHtml(result.gameName)}・AI推薦房</small><strong>${escapeHtml(result.roomNumber)}</strong></div><div class="confidence"><b>${escapeHtml(result.confidence)}</b><span>資料可信度</span></div></div><div class="signal">${escapeHtml(result.signal)}</div><div class="metrics"><div class="metric"><span>今日得分率</span><b>${formatNumber(result.metrics.todayRtp,"%")}</b></div><div class="metric"><span>近30天得分率</span><b>${formatNumber(result.metrics.monthRtp,"%")}</b></div><div class="metric"><span>今日樣本</span><b>${formatNumber(result.metrics.todayBet)}</b></div></div>${result.plan?`<div class="plan"><div><span>固定單位</span><b>${escapeHtml(result.plan.entry)}</b></div><div><span>整場停損</span><b>${escapeHtml(result.plan.stopLoss)}</b></div><div><span>達標停利</span><b>${escapeHtml(result.plan.takeProfit)}</b></div></div>`:""}<p class="note">${escapeHtml(result.note)}</p>`}async function loadGames(){const data=await api("/api/atg-x/games");games=data.games||[];if(!games.some(game=>game.gameName===selectedGame))selectedGame=games[0]?.gameName||"戰神賽特2"}async function boot(){try{const me=await api("/api/atg-x/me");if(!me.authenticated)return login();await loadGames();dashboard(me)}catch(error){login();toast(error.message)}}document.addEventListener("submit",async event=>{if(event.target.id!=="activate")return;event.preventDefault();const button=event.target.querySelector("button");button.disabled=true;try{await api("/api/atg-x/activate",{method:"POST",body:JSON.stringify({serial:event.target.serial.value,deviceId:deviceId()})});toast("序號啟用成功");await boot()}catch(error){toast(error.message)}finally{button.disabled=false}});document.addEventListener("click",async event=>{const card=event.target.closest("[data-game]");if(card){selectedGame=card.dataset.game;document.querySelectorAll(".game").forEach(node=>node.classList.toggle("active",node===card));const input=document.querySelector("#gameName");if(input)input.value=selectedGame;return}if(event.target.id==="analyze"){const button=event.target;button.disabled=true;button.textContent="掃描即時訊號中…";try{const data=await api("/api/atg-x/analyze",{method:"POST",body:JSON.stringify({gameName:selectedGame,bankroll:document.querySelector("#bankroll").value})});document.querySelector("#result").innerHTML=resultCard(data.result)}catch(error){toast(error.message)}finally{button.disabled=false;button.textContent="啟動 AI 訊號掃描"}}if(event.target.id==="logout"){await api("/api/atg-x/logout",{method:"POST",body:"{}"});login()}});document.addEventListener("pointermove",event=>{document.documentElement.style.setProperty("--mx",`${event.clientX}px`);document.documentElement.style.setProperty("--my",`${event.clientY}px`)});boot();
